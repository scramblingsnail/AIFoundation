# **K近邻法**

## **待解决问题**
给定N个训练样本，每个样本的特征维度数为K，共C类。
现有测试样本，需要判断测试样本的分类。

## **解决方案**
对每个测试样本，找到离它距离最近的 K 个训练样本（近邻），根据这K个近邻样本的类别进行多数投票，确定测试样本的类别。

关键在于如何找到 K 近邻。

### **暴力枚举法找K近邻**
计算测试样本到所有训练样本的距离，排序后选出距离最小的K个。

### **K Dimensional Tree 找K近邻**
将训练样本构建成一棵 KD Tree，减少搜索范围与复杂度。